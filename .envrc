#!/usr/bin/env bash

# Automatically activate the pinned Devbox environment when entering this repository.
eval "$(devbox generate direnv --print-envrc)"

# Keep Devbox's pinned toolchain ahead of host-managed shims (for example Volta).
export PATH="$DEVBOX_PACKAGES_DIR/bin:$PATH"
